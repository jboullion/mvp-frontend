import { make } from 'vuex-pathify';

const state = {};

const getters = {};

const actions = {};

const mutations = make.mutations(state);

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
