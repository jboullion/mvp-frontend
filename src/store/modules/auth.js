import { make } from 'vuex-pathify';

const state = {
  jwt: 'fake.token.string',
};

const getters = {};

const actions = {};

const mutations = {
  // increment(state) {
  //   state.count++;
  // },
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
